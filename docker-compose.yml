services:
  backend:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - config.env
    depends_on:
      - ml
      - redis

  ml:
    build: ./ml
    ports:
      - "6969:6969"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"