import { StyleSheet } from "react-native";

export const blur_style = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#7c3aed",
    justifyContent: "center",
    alignItems: "center",
  },
  glass: {
    width: 250,
    height: 120,
    borderRadius: 25,
    overflow: "hidden",
    justifyContent: "center",
    alignItems: "center",
  },
});
